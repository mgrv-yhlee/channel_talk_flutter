
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {

        google()
        jcenter()
        mavenCentral()

        maven { url 'https://maven.channel.io/maven2' } // Channel Maven repository
    }
}

rootProject.name = 'channel_talk'


